apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  # Using stringData allows you to put plaintext here; Kubernetes will base64 encode it automatically
  password: mypassword